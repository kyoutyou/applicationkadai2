<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h1>Editing Book</h1>
      <% if @book.errors.any? %>
        <%= @book.errors.count %>errors prohibited this obj from <br>
        being saved:
        <ul>
          <% @book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
        </ul>
      <% end %>
      
        <%= form_with model: @book do |f| %>
        <div class="field"></div>
          <p>Title</p>
            <%= f.text_field :title %>
            
        <div class="field"></div>
          <p>Opinion</p>
          <%= f.text_area :body %>
          
        <div class="actions"></div>  
          <%= f.submit 'Update Book' %>
        <% end %>
        
        <span>
          <%= link_to "Show", book_path(@book.id) %>
        </span>
        |
        <span>
          <%= link_to "Back", books_path %>  
        </span>
    </div>
  </div>
</div>
